export const UserRole = Object.freeze({
    ADMIN: "admin",
    USER: "user",
})

export const UserStatus = Object.freeze({
    PENDING: "pending",
    ACTIVE: "active",
    SUSPENDED: "suspended",
})
